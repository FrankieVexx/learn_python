# sets = a collection that is unordered and unindexed
# Do not accept duplicate values
utensils = {"fork", "spoon", "knife", "tin"}
dishes = {"bowl", "plate", "cup", "tin"}
#using function sets

#utensils.add()
#utensils.remove()
#utensils.clear()
#utensils.update(dishes)

#dinner_table = utensils.union(dishes)
#for x in dinner_table:
    #print(x)

# Compare elements in a set / find differences

#print(utensils.difference(dishes))
print(utensils.intersection(dishes)) #finds common item